import Layout from '../components/Layout';

const Home = () => {
  // Display events for today

  
  const bet = {
    odds: 0.2,
  };

  return <Layout>
    <div>1</div>
  </Layout>;
};

export default Home;
