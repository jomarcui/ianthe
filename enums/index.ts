export enum Roles {
  ADMIN = 0,
  USER = 1,
}

export enum Status {
  Ended = 'ENDED',
  Live = 'LIVE',
  Soon = 'SOON',
}

export enum Team {
  Home,
  Visitor,
}

export enum TransactionStatus {
  ACTIVE = 'ACTIVE',
  LOSS = 'LOSS',
  WIN = 'WIN',
}

export enum TransactionType {
  BET = 'BET',
  CREDIT = 'CREDIT',
  DEBIT = 'DEBIT',
}
